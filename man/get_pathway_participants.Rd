% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reactome.R
\name{get_pathway_participants}
\alias{get_pathway_participants}
\title{Reactome pathway participants (ChEBI mapping)}
\usage{
get_pathway_participants(pathway_ids = NULL, species = NULL, out_path = NULL)
}
\arguments{
\item{pathway_ids}{Character: Reactome pathway IDs to keep. If `NULL`, keeps
all pathways (subject to species filter).}

\item{species}{Character: species code (e.g. "HSA") or full name
(e.g. "Homo sapiens"). If `NULL`, returns all species.}

\item{out_path}{Character: optional CSV output path for the main table.}
}
\value{
A tibble with columns `pathway_id`, `pathway_name`, `pathway_url`,
  `species`, `chebi_ids` (list column). When writing to CSV, the list
  column is serialized by `write.csv`.
}
\description{
Retrieves ChEBI identifiers mapped to Reactome pathways and optionally
filters by species and/or pathway IDs. Returns a per-pathway list of ChEBI
IDs.
}
\examples{
.slow_doctest()
\dontrun{
tbl <- get_pathway_participants(species = "HSA")
head(tbl)
}
}
